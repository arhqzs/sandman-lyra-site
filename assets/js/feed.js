/* ============================================================
   SANDMAN LYRA — universal feed renderer
   Renders posts published from the Studio onto any page.
   Put  <div class="feed" data-sl-feed="SECTION"></div>  on a page
   (sections: live | journal | library | sports-mind | culture | media).
   Add  data-sl-filters  to also show a category filter bar (Live page).
   Reads window.SANDMAN_POSTS (generated by the publishing engine).
   ============================================================ */
(function () {
  "use strict";
  var DATA = window.SANDMAN_POSTS || window.SANDMAN_LIVE || [];
  var feeds = document.querySelectorAll("[data-sl-feed]");
  if (!feeds.length) return;

  var MONTHS = ["January","February","March","April","May","June","July","August","September","October","November","December"];
  var ORDER = ["World Cup","Betting","Backgammon","Boxing","Art","Philosophy","Friends","Travel"];

  function fmtDate(s) {
    var p = String(s || "").split("-");
    if (p.length !== 3) return s || "";
    return parseInt(p[2], 10) + " " + (MONTHS[parseInt(p[1], 10) - 1] || "") + " " + p[0];
  }
  function esc(t) {
    return String(t == null ? "" : t).replace(/[&<>"]/g, function (c) {
      return { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" }[c];
    });
  }
  window.__livePH = function () {
    return '<div class="ph"><svg width="38" height="38" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><rect x="3" y="5" width="18" height="14" rx="2"/><circle cx="9" cy="10" r="2"/><path d="M3 17l5-4 4 3 3-2 6 5"/></svg><small>Photo coming</small></div>';
  };
  function placeholder(kind) {
    var icon = kind === "video"
      ? '<svg width="40" height="40" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>'
      : '<svg width="38" height="38" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><rect x="3" y="5" width="18" height="14" rx="2"/><circle cx="9" cy="10" r="2"/><path d="M3 17l5-4 4 3 3-2 6 5"/></svg>';
    return '<div class="post-media"><div class="ph">' + icon + '<small>' + (kind === "video" ? "Video coming" : "Photo coming") + "</small></div></div>";
  }
  function mediaHTML(post) {
    if (post.type === "video") {
      if (post.embed) return '<div class="post-media"><div class="embed"><iframe src="' + esc(post.embed) + '" title="' + esc(post.title) + '" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen loading="lazy"></iframe></div></div>';
      if (post.video) return '<div class="post-media"><video controls preload="metadata" src="' + esc(post.video) + '"></video></div>';
      return placeholder("video");
    }
    if (post.type === "photo") {
      if (post.media) return '<div class="post-media"><img loading="lazy" alt="' + esc(post.title) + '" src="' + esc(post.media) + '" onerror="this.parentNode.innerHTML=window.__livePH()"></div>';
      return placeholder("photo");
    }
    return "";
  }
  function postHTML(post) {
    var url = location.origin + location.pathname + "#" + encodeURIComponent(post.id || "");
    var tags = (post.categories || []).map(function (c) {
      return '<button class="tag" data-cat="' + esc(c) + '">' + esc(c) + "</button>";
    }).join("");
    return '' +
      '<article class="post" id="' + esc(post.id) + '">' +
        '<div class="post-head">' +
          '<span class="post-date">' + esc(fmtDate(post.date)) + "</span>" +
          (post.location ? '<span class="post-loc">' + esc(post.location) + "</span>" : "") +
        "</div>" +
        mediaHTML(post) +
        '<div class="post-body">' +
          (post.title ? '<h2 class="post-title">' + esc(post.title) + "</h2>" : "") +
          (post.caption ? '<p class="post-caption">' + esc(post.caption) + "</p>" : "") +
          '<div class="post-foot">' +
            '<div class="post-tags">' + tags + "</div>" +
            '<div class="share-wrap">' +
              '<button class="share-btn" type="button" aria-label="Share">' +
                '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5l6.8 4M15.4 6.5l-6.8 4"/></svg> Share' +
              "</button>" +
              '<div class="share-menu" data-url="' + esc(url) + '" data-title="' + esc(post.title || "Sandman Lyra") + '">' +
                '<a target="_blank" rel="noopener" data-net="wa">WhatsApp</a>' +
                '<a target="_blank" rel="noopener" data-net="x">X / Twitter</a>' +
                '<a target="_blank" rel="noopener" data-net="fb">Facebook</a>' +
                '<button type="button" data-net="copy">Copy link</button>' +
              "</div>" +
            "</div>" +
          "</div>" +
        "</div>" +
      "</article>";
  }

  Array.prototype.forEach.call(feeds, function (feed) {
    var section = feed.getAttribute("data-sl-feed");
    var all = DATA.filter(function (p) { return (p.section || "live") === section; })
      .slice().sort(function (a, b) { return (b.date || "").localeCompare(a.date || ""); });
    var active = "All";
    var bar = null;

    if (feed.hasAttribute("data-sl-filters")) {
      bar = document.createElement("div");
      bar.className = "filter-bar";
      feed.parentNode.insertBefore(bar, feed);
    }
    function presentCats() {
      var s = {}; all.forEach(function (p) { (p.categories || []).forEach(function (c) { s[c] = 1; }); });
      return ORDER.filter(function (c) { return s[c]; });
    }
    function renderBar() {
      if (!bar) return;
      var btns = ['<button class="filter-btn' + (active === "All" ? " active" : "") + '" data-cat="All">All</button>'];
      presentCats().forEach(function (c) {
        btns.push('<button class="filter-btn' + (active === c ? " active" : "") + '" data-cat="' + esc(c) + '">' + esc(c) + "</button>");
      });
      bar.innerHTML = btns.join("");
    }
    function render() {
      var l = active === "All" ? all : all.filter(function (p) { return (p.categories || []).indexOf(active) !== -1; });
      feed.innerHTML = l.length ? l.map(postHTML).join("") : '<p class="feed-empty">Nothing posted here yet — check back soon.</p>';
    }
    feed._sl = { setActive: function (c) { active = c; renderBar(); render(); } };
    renderBar();
    render();
  });

  document.addEventListener("click", function (e) {
    var fb = e.target.closest ? e.target.closest(".filter-bar .filter-btn") : null;
    if (fb) {
      var theBar = fb.closest(".filter-bar");
      var theFeed = theBar && theBar.nextElementSibling;
      if (theFeed && theFeed._sl) { theFeed._sl.setActive(fb.getAttribute("data-cat")); window.scrollTo({ top: 0, behavior: "smooth" }); }
      return;
    }
    var tag = e.target.closest ? e.target.closest(".post-tags .tag") : null;
    if (tag) {
      var fEl = tag.closest("[data-sl-feed]");
      if (fEl && fEl._sl) { fEl._sl.setActive(tag.getAttribute("data-cat")); }
      return;
    }
    var sb = e.target.closest ? e.target.closest(".share-btn") : null;
    if (sb) {
      var menu = sb.parentNode.querySelector(".share-menu");
      var url = menu.getAttribute("data-url"), title = menu.getAttribute("data-title") || "Sandman Lyra";
      if (navigator.share) { navigator.share({ title: title, text: title, url: url }).catch(function () {}); return; }
      document.querySelectorAll(".share-menu.open").forEach(function (m) { if (m !== menu) m.classList.remove("open"); });
      menu.classList.toggle("open");
      return;
    }
    var net = e.target.closest ? e.target.closest(".share-menu [data-net]") : null;
    if (net) {
      var m2 = net.closest(".share-menu"), u = m2.getAttribute("data-url"), t = m2.getAttribute("data-title") || "Sandman Lyra", n = net.getAttribute("data-net");
      if (n === "copy") { if (navigator.clipboard) navigator.clipboard.writeText(u); net.textContent = "Copied ✓"; setTimeout(function () { net.textContent = "Copy link"; }, 1600); }
      else {
        net.setAttribute("href", n === "wa" ? "https://wa.me/?text=" + encodeURIComponent(t + " " + u)
          : n === "x" ? "https://twitter.com/intent/tweet?text=" + encodeURIComponent(t) + "&url=" + encodeURIComponent(u)
          : "https://www.facebook.com/sharer/sharer.php?u=" + encodeURIComponent(u));
      }
      return;
    }
    document.querySelectorAll(".share-menu.open").forEach(function (m) { m.classList.remove("open"); });
  });

  if (location.hash.length > 1) {
    var el = document.getElementById(decodeURIComponent(location.hash.slice(1)));
    if (el) el.scrollIntoView();
  }
})();
